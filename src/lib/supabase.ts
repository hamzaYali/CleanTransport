// Export just the tables, but don't create a client
// This avoids conflicts with the SSR client

// Database table names
export const TABLES = {
  TRANSPORTS: 'transports',
  ANNOUNCEMENTS: 'announcements',
  USERS: 'users',
}; 